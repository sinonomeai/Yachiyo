# Day 1

- 借助DS解析需求，大致确定项目结构以及技术栈并创建项目
- 了解单token登录流程
- VSC接入DS替代copilot

# Day 2

- 基于视频参考，将原生 JavaScript 项目的登录/注册模块迁移至 React，并优化 UI/UX
- Next.js 中，组件默认为服务端组件（Server Component）,无法使用 React Hooks 或交互事件。需要在文件顶部添加 'use client' 指令，才能将其显式声明为客户端组件（Client Component）

# Day 3

- 本次开发收获：
  1. 优化登录/注册模块切换逻辑
     - 使用 CSS transition 实现平滑切换动画
     - 通过状态管理控制面板显示/隐藏

  2. 借助 AI 实现背景图鼠标跟随浮动效果
     - 基于 requestAnimationFrame 实现物理惯性动画
     - 封装为可复用的 useMouseFollower Hook

  3. 深入理解 object-fit 属性
     - 核心要点：img 元素必须有明确的宽度和高度
     - 配合 position: absolute + inset: 0 可完美铺满父容器

# Day 4

**1. 表单处理与验证**

- **zod**：用 `z.object`、`.email()`、`.min()`、`.refine()` 处理表单验证规则
- **react-hook-form**：通过 `register`、`handleSubmit`、`errors`、`isSubmitting` 管理表单状态，处理表单提交逻辑
- **zodResolver**：连接 zod 与 react-hook-form

**2. JWT 与 Cookie 存储**

- 用 `jwt.sign({ userId, email }, SECRET, { expiresIn: "7d" })` 生成 token
- 存入 httpOnly Cookie并配置

**3. Next.js 中间件路由保护**

- 了解Next中间件工作流程
- 用 `NextRequest` 获取请求信息，`NextResponse` 控制响应，在请求到达页面前统一完成鉴权。

# Day 5

**因浏览器与Next的缓存策略导致的组件不重新挂载问题**

- 浏览器直接从内存中恢复页面快照（bfcache特性），而不重新加载HTML/CSS或执行JavaScript

- 因此事件监听器、定时器、useEffect 等初始化逻辑不会重新执行

- 当前暂时使用 replace 跳转解决

# Day 6

**1. 优化登录注册页组件以及样式**

+ 封装Input组件
+ 组件不重新挂载的问题只存在于开发环境中，生产环境并无问题

**2. 编写主页面布局以及样式**

**3. 引入阿里字体图标丰富样式**

# Day 7

1. **侧边栏交互优化：添加收起/展开过渡动画，提升用户体验**

2. **封装 Input 输入组件：**
   - 集成自定义 Button 图标按钮
   - textarea 高度随内容自动撑开（scrollHeight 检测）
   - 最大高度限制 300px，超出显示滚动条
   - 输入状态控制发送按钮禁用逻辑

# Day 8

1. **实现输入功能 UI 按钮**

完成了输入区域的 UI 按钮组件开发，支持用户输入交互。按钮样式与整体设计系统保持一致，后续可根据业务需求扩展具体功能逻辑。

2. **实现历史消息区域的 Flex 布局滚动**

在 Flex 布局下实现了历史消息记录的溢出滚动效果。核心要点如下：
- 使用 `flex-1` 让历史消息区域占满剩余空间
- 关键设置：添加 `min-h-0` 覆盖 Flex 子项默认的 `min-height: auto` 属性，避免内容撑开容器导致滚动失效
- 配合 `overflow-y-auto` 实现内容超出时的滚动效果

该方案无需 JavaScript 计算高度，纯 CSS 实现动态高度适配。

3. **实现响应式侧边栏与主内容区布局**

完成了页面级的响应式布局适配：
- 侧边栏响应式：监听窗口宽度变化（断点 1024px），窗口小于断点时自动收起，大于断点时自动展开
- 用户手动操作优先级：用户手动收起侧边栏后，不再受自动展开逻辑影响，直至用户主动展开，保证交互预期
- 主对话区域：根据侧边栏状态自适应宽度，实现流畅的布局切换

4. **实现历史消息区域底部的文字渐隐效果**

在历史消息列表底部实现了文字渐隐效果，用于提示用户下方仍有更多内容。核心实现方式如下：
- 使用伪元素 `::after` 配合绝对定位，覆盖在列表底部区域
- 通过 `linear-gradient` 渐变从透明过渡到背景色（如 `transparent` → `#1a1a2e`），营造自然淡出的视觉效果
- 设置 `pointer-events: none` 确保渐变层不干扰用户的点击滚动操作

5. **！！！！！！！暂时**实现了滚动条移入出现移出消失的效果，但是还存在触发界限有误的问题

# Day 9

1. **引入 Zustand 管理侧边栏状态**
   - 使用 Zustand 统一管理侧边栏的展开/收起状态，实现跨组件状态共享
2. **根据侧边栏展开状态动态调整主内容标题栏盒子宽度**
   - 监听 isExpanded 状态，动态计算主内容区域宽度
   - 利用calc动态计算宽度
   - 添加 transition 过渡效果，实现平滑的宽度变化动画
3. **实现移动端侧边栏绝对定位展开及遮罩层收起**
   - 移动端（< md 断点）侧边栏改为 absolute 定位，脱离文档流
   - 展开时显示半透明黑色遮罩层，覆盖主内容区域（fixed inset-0）
   - 点击遮罩层自动收起侧边栏，提升移动端交互体验

# Day 10

1. **借助ai直接生成ai与用户的聊天气泡**
2. **同时自编写ai头像样式**
3. **使用ai sdk完成了简易对话模块**
4. **使用pg+pgvector搭建文档、用户、会话、历史消息数据库**

# Day 11

1. **编写从用户发消息到路由跳转进行子路由渲染再到最终输出文本的数据流程图**
2. **完成跳转会话渲染历史聊天记录模块**
3. **利用scrollIntoView实现会话输出平滑滚动到最新消息位置效果**
4. **用zustand持久化保存用户会话列表**
5. **用tanstack query实现历史聊天记录加载缓存**

# Day 12

1. **实现 RAG（检索增强生成）功能**
   - 将 Markdown 文档分块后通过 pgvector 向量化存储
   - 用户提问时先向量检索相关文档片段，再拼接为 system prompt 上下文
   - 支持按知识库和选中文档范围限定检索源

2. **AI 查询改写优化检索命中率**
   - 用户口语化问题先调用 DeepSeek 改写为专业关键词组合
   - 改写后的问题再进行向量检索，大幅提升命中率

3. **完成聊天机器人核心模块**
   - 集成 `@ai-sdk/react` 的 `useChat`，实现流式聊天
   - 前端 `sendMessage` 携带 `knowledgeBaseId` 和 `documentIds`
   - 流完成后自动保存用户/AI 消息到 PostgreSQL

# Day 13

1. **优化历史聊天页面 UI**
   - 重构会话列表与聊天区域布局
   - 历史消息使用 `flex-1 min-h-0` 实现纯 CSS 自适应滚动
   - `scrollIntoView` 平滑滚动到最新消息位置

2. **Loading 动画设计**
   - 使用 GSAP 实现六边形网格渐现→缩小的加载动画
   - SVG `<use>` 元素 + 二维数组生成六边形矩阵
   - 动画完成后背景同步淡出

3. **修复 SVG transform-box 导致的缩放原点问题**
   - SVG `<use>` 的 `x`/`y` 属性导致 `transform-origin: center` 指向视口中心而非元素中心
   - 用外层 `<g transform="translate(x,y)">` 替代 `<use x={x} y={y}>`，配合 `transform-box: fill-box` 解决

# Day 14

1. **知识库模块 CRUD 开发**
   - 知识库创建/修改/删除完整前后端
   - 文档上传（.md 格式）、重命名、删除
   - 上传进度条：逐文件上传，实时更新任务状态

2. **提取 SessionMenu 和 KbMenu 组件**
   - 为会话列表右侧添加 `···` 展开按钮，提供重命名和删除功能
   - 知识库列表复用相同交互模式
   - 弹窗位置自适应：检测底部空间，空间不足时向上展开

3. **删除确认改为 Popup 弹窗**
   - 文档删除从"两步点击确认"改为 Popup 弹窗确认
   - 弹窗展示文档名，提供确认/取消按钮，点击遮罩层可关闭

# Day 15

1. **会话管理从 Zustand 重构为 React Query**
   - 创建 `useSessionsData` Hook：`useSessions`（查询）+ `useAddSession`/`useRemoveSession`/`useUpdateSessionTitle`（变更）
   - 所有 mutation 实现乐观更新：`onMutate` 立即更新缓存，`onError` 回滚，`onSettled` 重新验证
   - 删除旧 `useSessionsStore`，Zustand 仅保留客户端 UI 状态

2. **修复会话标题同步导致列表乱序**
   - 点击旧会话时，`useMessages` 获取到已有标题后触发 `updateSessionTitle`
   - PUT API 更新 `updated_at` → 列表按 `updated_at DESC` 排序 → 会话跳到顶部
   - 修复：仅在 `sessionData.title !== localSession?.title` 时才调用更新

3. **修复 init API 时序竞态**
   - `updateTitleAsync`（AI 标题生成，耗时 6-9s）被 `await` 阻塞响应
   - 导致前端跳转到聊天页后 stream API 立即调用时 DB 中尚无 session
   - 修复：session 先写入 DB 立即返回，标题生成改为后台异步

# Day 16

1. **深度思考模式集成**
   - 查阅 DeepSeek API 文档，`deepseek-reasoner` + `providerOptions.deepseek.thinking` 控制思考模式
   - `deepThinking` 开关通过 `@ai-sdk/deepseek` 的 `providerOptions` 传递
   - 前端 Input 组件添加深度思考/智能搜索按钮，激活时蓝色高亮

2. **Reasoning 思考过程渲染**
   - DeepSeek R1 推理模型的 `reasoning_content` 以 `part.type === "reasoning"` 形式返回
   - 创建 `ReasoningBlock` 组件：可折叠区块，左侧竖线装饰，灰色文字
   - 流式输出时默认展开并显示闪烁光标，完成后默认折叠

3. **聊天路由重构：`/yachiyo/[id]` → `/yachiyo/chat/[id]`**
   - `[id]` 目录移入 `chat/` 下，Chat 组件从子目录提升为同级文件
   - 批量更新所有 URL 引用（List、SessionMenu、page.tsx 跳转）

# Day 17

1. **组件模块化拆分**
   - `docbase/[baseId]/` 拆分出 `UploadSection`（上传+进度）、`DocumentList`（列表+删除弹窗）
   - 子组件自管理状态（上传 tasks、编辑 rename、删除 target），减少 props 传递，降低耦合
   - `List.tsx` 拆分为 `SessionMenu/`（`SessionMenu` + `RenameSession`）和 `KbMenu/`（`KbMenu` + `EditKnowledgeBase` + `CreateKnowledgeBase`）
   - `stream/route.ts` 拆分出 `rag/buildRagContext`（RAG 检索 + 查询改写）和 `init/updateTitleAsync`（标题生成）

2. **API 函数文件化**
   - `rewriteQuery` + `buildRagContext` + `formatVector` → `stream/rag/buildRagContext.ts`
   - `updateTitleAsync` + `generateTitle` → `init/updateTitle/updateTitleAsync.ts`
   - 主 `route.ts` 从 103-177 行精简到 36-86 行

# Day 18

1. **Loading 动画主题色适配**
   - 背景: `#000` → `#14141f`（项目主背景色）
   - 六边形填充: `#171717` → `#1a1a2e`（卡片底色）
   - 描边: 荧光绿 `#17f700` → 项目蓝 `#5b8def`
   - 淡出目标色同步调整

2. **响应式布局细节优化**
   - 侧边栏展开时主内容区自适应宽度
   - 移动端遮罩层点击关闭，用户手动操作优先级高于自动响应

3. **403/404 页面风格统一**
   - 404 页面：深色主题 + "This page has wandered into the shadows" + 返回 Yachiyo 按钮
   - 403 页面：Go to Login + Return to Yachiyo 双按钮
   - 未登录用户访问受保护路由重定向到 403

# Day 19

1. **修复思考中占位不显示问题**
   - `sendMessage` 后 assistant 消息尚未加入 `messages` 数组时，`lastMessage` 是 user 消息
   - ThinkingDots 仅存在于 assistant 渲染分支，导致不显示
   - 修复：Chat 组件层新增兜底占位——`status` 为 submitted/streaming 且 `lastMessage.role === "user"` 时渲染独立的思考气泡

2. **历史记录加载态**
   - Chat 组件添加 `isLoading` prop，从 `useMessages` 传递
   - 历史消息加载中页面显示"加载中" + 弹跳点动画，避免空白

3. **复选框自定义样式**
   - KnowledgeModal 中原生 checkbox 替换为 `sr-only peer` + 视觉 span
   - Tailwind `peer-checked:` 控制选中态样式，保留原生无障碍语义

# Day 20

1. **图标系统 SVG 化**
   - `docbase/[baseId]/` 中文档图标、上传图标、空状态图标改为内联 SVG
   - `docbase/page.tsx` 知识库图标改为展开书本 SVG
   - 逐步减少对 iconfont 的依赖
2. **性能优化：React.memo + zustand selector**
   - `SessionMenu`、`KbMenu`、`RenameSession` 包裹 `React.memo`
   - `useSessionsStore` 无 selector → 精确 selector 订阅特定字段
   - `useCallback` 稳定事件处理器，消除不必要重渲染
3. **项目文档完善**
   - 更新结构文档：完整反映当前项目结构、技术栈、关键依赖
   - 重写 README：项目简介、功能列表、快速开始、数据库初始化脚本

